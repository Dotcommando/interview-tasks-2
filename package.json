{
  "name": "interview-tasks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "arrays": "npm run build && node dist/task-1---identical-elements-in-arrays",
    "promisify": "npm run build && node dist/task-2---promisify",
    "brackets": "npm run build && node dist/task-3---matching-brackets",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "17.0.8",
    "rimraf": "3.0.2",
    "typescript": "4.5.4"
  },
  "dependencies": {
  }
}
